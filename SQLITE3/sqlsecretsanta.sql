CREATE TABLE users(
                    user_id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
                    name TEXT NOT NULL,
                    preferences TEXT NOT NULL,
                    budget INTEGER NOT NULL);

